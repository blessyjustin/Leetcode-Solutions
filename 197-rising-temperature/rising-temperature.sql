/* Write your T-SQL query statement below */
SELECT ID FROM ( SELECT ID,TEMPERATURE,RECORDDATE,LAG(TEMPERATURE)OVER (ORDER BY RECORDDATE) AS PREV_TEMP,LAG(RECORDDATE)OVER (ORDER BY RECORDDATE)AS PREV_DATE FROM WEATHER) AS T WHERE TEMPERATURE>PREV_TEMP AND DATEDIFF(DAY,PREV_DATE,RECORDDATE)=1